{
  "name": "portal-toit",
  "version": "1.0.0",
  "description": "Portal TOIT - Single Sign-On e área do cliente unificada",
  "scripts": {
    "build": "cd backend && npm ci && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "test": "cd backend && npm run test",
    "lint": "cd backend && npm run lint"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}